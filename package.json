{
  "name": "l2-maths-archive",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "install:mpsi": "node scripts/install-mpsi-maths-from-rar.mjs",
    "deploy": "npm run build && firebase deploy",
    "deploy:hosting": "npm run build && firebase deploy --only hosting"
  },
  "dependencies": {
    "astro": "^5.15.0",
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "firebase-tools": "^14.27.0",
    "playwright": "^1.57.0"
  }
}
