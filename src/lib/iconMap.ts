import { LucideIcon, SquareFunction, Sigma, Orbit, Divide, Percent, Calculator, BookOpen } from 'lucide-react';

export const iconMap: Record<string, LucideIcon> = {
  SquareFunction,
  Sigma,
  Orbit,
  Divide,
  Percent,
  Calculator,
  BookOpen,
};
